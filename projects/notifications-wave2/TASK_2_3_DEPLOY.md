# Task 2.3 — План внедрения

**Дата:** 2026-04-20  
**Статус:** 🔧 Задеплоено на тестовую среду (2026-04-21). Ожидает тестирования и переноса на прод.  
**Пакет:** CTI  
**Дизайн:** [TASK_2_3_DESIGN.md](TASK_2_3_DESIGN.md)

---

## Что делаем

Когда клиент отвечает на обращение → инженер получает email с текстом письма клиента.  
Работает при **любом статусе** обращения (не только «Получен ответ»).

---

## Готовые артефакты

| Файл | Статус |
|------|--------|
| [src/UsrLatestCustomerEmailGenerator.cs](src/UsrLatestCustomerEmailGenerator.cs) | ✅ Готов |
| [src/UsrSendEmailToCaseOwnerOnReply.cs](src/UsrSendEmailToCaseOwnerOnReply.cs) | ✅ Готов |
| BPMN ScriptTask (код ниже) | ✅ Готов |

---

## Шаги внедрения

### Шаг 1: Создать справочник `UsrEmployeeNotificationEventType`

Конфигурация → пакет CTI → Добавить → **«Объект»**.

| Поле | Значение |
|------|---------|
| Заголовок | Тип уведомления сотрудника |
| Код | UsrEmployeeNotificationEventType |
| Родительский объект | BaseLookup |
| Пакет | CTI |

При выборе `BaseLookup` система покажет всплывающее окно подтверждения — нажать «Да».  
Колонки Id, Name, Description **наследуются автоматически** — добавлять вручную не нужно.

Сохранить и опубликовать объект.

**Зарегистрировать как справочник:**  
Дизайнер системы → «Справочники» → «Добавить справочник»:

- Название: `Тип уведомления сотрудника`
- Объект: `UsrEmployeeNotificationEventType`
- Сохранить

Данные (добавить через раздел «Справочники» после регистрации):

| Название | Описание |
|----------|----------|
| CustomerReply | Ответ клиента |
| OwnerAssigned | Назначение ответственного |
| GroupAssigned | Назначение на группу |
| StatusChanged | Смена статуса |

**Привязать данные к пакету CTI:**  
Конфигурация → пакет CTI → Добавить → «Данные»:

1. Объект: `UsrEmployeeNotificationEventType`
2. Тип установки: **Установка**
3. Вкладка «Настройка колонок» — выбрать: **Id**, **Name**, **Description**
4. Вкладка «Привязанные данные» → «Добавить» → выбрать все 4 записи
5. Сохранить

---

### Шаг 2: Создать справочник `UsrEmployeeNotificationRecipientType`

Конфигурация → пакет CTI → Добавить → **«Объект»**.

| Поле | Значение |
|------|---------|
| Заголовок | Тип получателя уведомления сотрудника |
| Код | UsrEmployeeNotificationRecipientType |
| Родительский объект | BaseLookup |
| Пакет | CTI |

Сохранить и опубликовать объект.

**Зарегистрировать как справочник:**  
Дизайнер системы → «Справочники» → «Добавить справочник»:

- Название: `Тип получателя уведомления сотрудника`
- Объект: `UsrEmployeeNotificationRecipientType`
- Сохранить

Данные (добавить через раздел «Справочники» после регистрации):

| Название | Описание |
|----------|----------|
| Owner | Ответственный |
| Group | Группа |
| Role | Роль |

**Привязать данные к пакету CTI:**  
Конфигурация → пакет CTI → Добавить → «Данные»:

1. Объект: `UsrEmployeeNotificationRecipientType`
2. Тип установки: **Установка**
3. Вкладка «Настройка колонок» — выбрать: **Id**, **Name**, **Description**
4. Вкладка «Привязанные данные» → «Добавить» → выбрать все 3 записи
5. Сохранить

---

### Шаг 3: Создать объект `UsrEmployeeNotificationRule`

Конфигурация → пакет CTI → Добавить → **«Объект»**.

| Поле | Значение |
|------|---------|
| Заголовок | Правила уведомлений сотрудника по обращению |
| Код | UsrEmployeeNotificationRule |
| Родительский объект | BaseEntity |
| Пакет | CTI |

Колонки (тип «Справочник» → указать источник данных):

| Название колонки | Заголовок | Тип | Обязательное | Ссылка на |
| --------------- | --------- | --- | ----------- | --------- |
| UsrCaseStatus | Статус обращения | Lookup | нет | CaseStatus |
| UsrCaseCategory | Категория обращения | Lookup | нет | CaseCategory |
| UsrEventType | Тип события | Lookup | нет | UsrEmployeeNotificationEventType |
| UsrEmailTemplate | Шаблон письма | Lookup | да | EmailTemplate |
| UsrRecipientType | Тип получателя | Lookup | да | UsrEmployeeNotificationRecipientType |
| UsrRole | Роль | Lookup | нет | SysAdminUnit |
| UsrIsActive | Активно | Boolean | — | Значение по умолчанию = Да |

> ⚠️ Для Boolean-колонки `UsrIsActive` признак «Обязательное» недоступен по платформе. Установить **«Значение по умолчанию» = Да (константа)** — новые записи будут создаваться с `UsrIsActive = true`.

Сохранить и опубликовать объект.

**Зарегистрировать как справочник:**

Дизайнер системы → «Справочники» → «Добавить справочник»:

- Название: `Правила уведомлений сотрудника по обращению`
- Объект: `UsrEmployeeNotificationRule`
- Сохранить

---

### Шаг 4: Создать C# схему `UsrLatestCustomerEmailGenerator`

Конфигурация → пакет CTI → Добавить → **«Исходный код»**.

- Название: `UsrLatestCustomerEmailGenerator`
- Вставить код из [src/UsrLatestCustomerEmailGenerator.cs](src/UsrLatestCustomerEmailGenerator.cs)
- Сохранить и опубликовать

**Зарегистрировать макрос** через **Data пакета CTI** (UI для этого не предусмотрен):

1. Выполнить SQL на текущей среде (UI для этого не предусмотрен):
   ```sql
   INSERT INTO "EmailTemplateMacros" ("Name", "ParentId", "ColumnPath")
   VALUES (
       'UsrLatestCustomerEmailGenerator',
       (SELECT "Id" FROM "EmailTemplateMacros" WHERE "Name" = '@Invoke' LIMIT 1),
       'BPMSoft.Configuration.UsrLatestCustomerEmailGenerator'
   );
   ```
2. Раздел «Конфигурация» → пакет CTI → «Добавить» → **«Данные»**
3. Объект: `EmailTemplateMacros`
4. Тип установки: **Установка**
5. Вкладка «Настройка колонок» — выбрать: **Id**, **Name**, **ColumnPath**, **Parent**
6. Вкладка «Привязанные данные» → «Добавить» → найти запись `UsrLatestCustomerEmailGenerator` → выбрать
7. Сохранить — при установке пакета на любую среду регистрация выполнится автоматически

---

### Шаг 5: Создать C# схему `UsrSendEmailToCaseOwnerOnReply`

Конфигурация → пакет CTI → Добавить → **«Исходный код»**.

- Название: `UsrSendEmailToCaseOwnerOnReply`
- Вставить код из [src/UsrSendEmailToCaseOwnerOnReply.cs](src/UsrSendEmailToCaseOwnerOnReply.cs)
- Сохранить и опубликовать

---

### Шаг 6: Клонировать шаблон email и добавить макрос

**Дизайнер системы → Справочники → «Шаблоны сообщений»** → найти шаблон:
**«Отправка уведомления ответственному о смене статуса обращения на Получен ответ (RU)»**
(UId начинается с `18834f34` — для поиска через БД: `SELECT "Id", "Name" FROM "EmailTemplate" WHERE "Id"::text ILIKE '18834f34%'`)

1. Выделить шаблон → меню **«Действия» → «Копировать»**
2. Открыть копию, задать:
   - Название: `Уведомление инженеру — ответ клиента`
   - Тема письма: `Кейс #[#Number#] — новый ответ клиента`
3. На детали «Шаблон письма» нажать **«Редактировать»** → откроется дизайнер контента
4. Кликнуть на тело письма → в панели инструментов нажать иконку **«Редактировать исходный HTML»** (последняя справа, `</>`)
5. Заменить всё содержимое тела на:
   ```html
   <strong>Кейс #[#Number#] — новый ответ клиента<br />
   Контрагент: [#Account#]<br />
   Приоритет: [#Priority#]<br />
   Тема обращения: [#Subject#]<br />
   Оборудование: [#ConfItem#]<br />
   Группа ответственных: [#Group#]<br />
   Ссылка на кейс:&nbsp;<a href="https://bpm.cti.ru/Navigation/Navigation.aspx?schemaName=Case&amp;recordId=[#Id#]" style="color: #ff0004;">[#Number#]</a></strong><br />
   <br />
   <strong>Сообщение клиента:</strong><br />
   #@Invoke.UsrLatestCustomerEmailGenerator#
   ```
6. Сохранить шаблон
7. Запомнить UId новой записи через БД:

   ```sql
   SELECT "Id", "Name" FROM "EmailTemplate" WHERE "Name" ILIKE '%инженер%';
   ```

**Привязать шаблон к пакету CTI:**

Конфигурация → пакет CTI → «Добавить» → «Данные»:
1. Объект: `EmailTemplate`
2. Тип установки: **Установка**
3. Вкладка «Настройка колонок» — отметить: **Id**, **Name**, **Body**, **Тело в HTML-формате** (IsHtmlBody), **Тема письма** (Subject), **Источник макросов** (Object), **Макросы** (Macros), **TemplateConfig**, **Тип конфига** (ConfigType), **Тип шаблона** (TemplateType)
4. Вкладка «Привязанные данные» → «Добавить» → найти «Уведомление инженеру — ответ клиента» → выбрать
5. Сохранить

> ⚠️ Шаг 6 выполнять **после** шага 4 (публикация C# класса и регистрация в EmailTemplateMacros).
> ⚠️ Кнопка «Пользовательский макрос» в дизайнере контента **не показывает** invokable-макросы — вставлять только через HTML-редактор строкой `#@Invoke.UsrLatestCustomerEmailGenerator#`.
> ⚠️ `[#Subject#]` — правильный макрос для темы обращения (подтверждено в существующих шаблонах системы).

---

### Шаг 7: Добавить данные в `UsrEmployeeNotificationRule`

По документации: сначала создать записи через UI, затем привязать к пакету через «Данные».

**7.1 Создать записи через UI**

Дизайнер системы → Справочники → найти «Правила уведомлений сотрудника по обращению» → открыть → добавить записи:

**Правило 1 — Ответственному:**

| Поле | Значение |
|------|---------|
| Тип события | CustomerReply |
| Шаблон письма | Уведомление инженеру — ответ клиента |
| Тип получателя | Owner |
| Активно | Да (по умолчанию) |
| Остальные | пусто |

**Правило 2 — Роли «1-я линия»:**

| Поле | Значение |
|------|---------|
| Тип события | CustomerReply |
| Шаблон письма | Уведомление инженеру — ответ клиента |
| Тип получателя | Role |
| Роль | 1-я линия поддержки |
| Активно | Да (по умолчанию) |
| Остальные | пусто |

> ⚠️ Правило 2 заменяет хардкод CC из `UsrProcess_0c71a12CTI5`. Добавить ДО шага 9.

**7.2 Привязать записи к пакету CTI**

Раздел «Конфигурация» → пакет CTI → «Добавить» → «Данные»:
1. Объект: `UsrEmployeeNotificationRule`
2. Тип установки: «Установка»
3. Вкладка «Настройка колонок» — выбрать все колонки, **обязательно включая Id**
4. Вкладка «Привязанные данные» → «Добавить» → выбрать оба созданных правила
5. Сохранить

---

### Шаг 8: Создать BPMN `UsrSendEmailToCaseOwnerOnReplyProcess`

Дизайнер системы → **Библиотека процессов** → «Добавить процесс».

> ⚠️ Пакет CTI выбирается не при нажатии кнопки «Добавить», а в свойствах процесса внутри дизайнера.

**8.1 Свойства процесса** (правая панель → вкладка «Настройки»):

- Имя: `UsrSendEmailToCaseOwnerOnReplyProcess`
- Заголовок: `Отправка email сотруднику при ответе клиента`
- Пакет: `CTI`

**8.2 StartSignal** — кликнуть на стартовый элемент (зелёный круг) → нажать кнопку ⚙ → выбрать **«Сигнал»**:

- «Сигнал какого типа получен?» → **«Получен сигнал от объекта»**
- Объект: **Активность**
- «Какое событие должно произойти?» → **«Добавление записи»**
- Фильтры добавленной записи (кнопка «Действия» в блоке условий):
  - `Тип = Email`
  - `Тип сообщения = Входящее`
  - `Обращение заполнено`
  - `Обработано процессами сервиса = Нет`
- Галочка **«Выполнять следующие элементы в фоне»** — включить

> ⚠️ Поля «Направление» в фильтрах нет — используется «Тип сообщения = Входящее».
> ⚠️ Имя элемента сигнала смотреть в правой панели → поле **«Имя»** (по умолчанию `StartSignal1`) — оно нужно в коде ScriptTask.

**8.3 ScriptTask** — добавить элемент **«Задание-сценарий»**:

Элемент находится в группе **«Действия системы»** — нажать на стрелку вниз рядом с синим квадратом на панели инструментов внизу → выбрать «Задание-сценарий». Либо через контекстное меню сигнала — кнопка с «волшебной палочкой».

- Название: `SendEmailToCaseOwnerScriptTask`
- Код (заменить `return true;` на):

```csharp
var notifier = new UsrSendEmailToCaseOwnerOnReply(UserConnection);
notifier.ActivityId = Get<Guid>("StartSignal1.RecordId");
notifier.Execute();
return true;
```

> `StartSignal1` — имя элемента сигнала из поля «Имя» в его настройках. Если отличается — заменить.

**8.4 Добавить пространство имён**

В правой панели свойств процесса → вкладка **«Методы»** → нажать **«Добавить пространство имён +»** → ввести:

```
BPMSoft.Configuration
```

> ⚠️ Без этого пространства имён компилятор не найдёт класс `UsrSendEmailToCaseOwnerOnReply` и публикация завершится ошибкой `CS0246`. Это стандартное требование платформы для любого процесса, использующего пользовательские C# классы из пакета.

**8.5 Сохранить и опубликовать**

Кнопка «Сохранить» → затем **«Действия» → «Опубликовать»**.

> ⚠️ ScriptTask требует обязательной публикации — без неё процесс не запустится.
> ⚠️ Не активировать до шага 9.

---

### Шаг 9: Переключение (деплой)

Выполнять последовательно, без паузы:

1. **Убедиться** что Правило 2 (роль «1-я линия») добавлено в UsrEmployeeNotificationRule
2. **Активировать** `UsrSendEmailToCaseOwnerOnReplyProcess` в дизайнере процессов
3. **Деактивировать** `UsrProcess_0c71a12CTI5` в дизайнере процессов

Перезапуск Kestrel **не нужен** — C# схемы (Исходный код) подхватываются после публикации. BPMN активируется мгновенно.

---

## Rollback

1. Реактивировать `UsrProcess_0c71a12CTI5`
2. Деактивировать `UsrSendEmailToCaseOwnerOnReplyProcess`
3. Время: ~1 минута, без деплоя пакета

---

## Проверка после внедрения

1. Отправить тестовое письмо на ящик обращений (с любым статусом: Новое, В работе, Получен ответ)
2. Убедиться что инженер получил email
3. Убедиться что в письме есть блок «Сообщение клиента» с текстом
4. Убедиться что роль «1-я линия» тоже получила уведомление
5. Проверить лог `CaseNotification` на ошибки

---

## Важные зависимости

- `UsrLatestCustomerEmailGenerator` получает тело письма по `ActivityId` конкретного входящего письма (не «последнего» — исключает race condition)
- `SendEmailFromTo` не читает GetCaseData → recipient передаётся явно через ESQ `Case.Owner.Email`
- `ServiceProcessed=false` в фильтре StartSignal корректен: INSERT Activity происходит до того как `ReopenCaseAndNotifyAssignee.Run()` ставит `true`
